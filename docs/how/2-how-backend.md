# How is the backend made?

The backend was not made at all.

We are using [Supabase](https://supabase.com/) which is a Backend as a Service.

We have a free Supabase account which contains a postgres database to save the conversations.

Supabase also has an API-gateway to query this data using REST calls.